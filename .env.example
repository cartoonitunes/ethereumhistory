# Database connection string (Vercel Postgres / Neon / Supabase / local PostgreSQL)
# Prefer POSTGRES_URL (used by the app). DATABASE_URL is accepted as a fallback.
POSTGRES_URL="postgresql://postgres:postgres@127.0.0.1:5433/ethereumhistory"
DATABASE_URL=""

# Optional: Etherscan API key (ABI + contract creation enrichment)
ETHERSCAN_API_KEY=""

# RPC endpoint (Alchemy recommended for token logos + metadata)
ETHEREUM_RPC_URL="https://eth-mainnet.g.alchemy.com/v2/your-api-key"
